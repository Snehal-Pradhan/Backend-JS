
import {z} from zod ;


export const validate = (schema) => {
    const {name,email,role} = schema.parse(
        {
            name : req.body.name,
            email : req.body.email,
            role: req.body.role
        }
        req.body.name = 
        next()

    )
}